- config to start and end ctf

- move countries from api to frontend or /static

- resolve TODOs left into the code
- when finished endpoints, review needed data and revome `RETURNIN *;` and similar when not needed
- sort constants alphabetically

- nginx
- redis
- rate limiter middleware

- webhook to discord for first bloods
- deletable instances (from admin)
- auto cleaner thread of dead instances
- deletable submissions (from admin)
- endpoint + cli cmd to parse a category/challenge file with all infos
- link to join team
- kick user from team
- maybe set default starting points for challenges as config
- flag submit that checks for all challenges and mark solved the one of the correct flag (credits to: midnightsun)
- writeups for challs into the platform (visible after ctf ends)
- signed flags
